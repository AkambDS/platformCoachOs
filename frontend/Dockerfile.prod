FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
ARG VITE_API_BASE_URL=""
ENV VITE_API_BASE_URL=$VITE_API_BASE_URL
RUN npm run build

# Final image just holds the dist — nginx serves it via volume
FROM alpine:3.19
COPY --from=builder /app/dist /app/dist
